import React from "react";
import { Layout } from "antd";

const { Sider } = Layout;
type Props = {};
export const EditBlockSidebar = (props: Props) => {
  return (
    <Layout>
      <Sider></Sider>
    </Layout>
  );
};
